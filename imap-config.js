// imap-config.js

module.exports = {
  host: 'imap.creators.goated', // IMAP server host
  port: 993, // default IMAP port
  secure: true, // use TLS
  auth: {
    user: 'your-email@creators.goated', // your email address
    pass: 'your-email-password' // your email password
  }
};